site:
  about:
    siteName: Plume Driver
    siteDescription: Provides a type-safe interface for connecting and writing to various graph databases based on the code-property graph schema.
Editorial:
  primaryColor: '#02a6f2'
  legacySearch: false
  metaComponents:
    - type: 'orchidSearch'
  social:
    github: 'plume-oss/plume-driver'
  menu:
    - type: 'separator'
      title: 'API Docs'
    - type: 'sourcedocPages'
      moduleType: 'kotlindoc'
      node: 'classes'
      asSubmenu: true
      submenuTitle: 'Classes'
    - type: 'sourcedocPages'
      moduleType: 'kotlindoc'
      node: 'packages'
      asSubmenu: true
      submenuTitle: 'Packages'

kotlindoc:
  sourceDirs:
    - './../../main/kotlin'

services:
  publications:
    stages:
      - type: 'githubPages'
        username: 'DavidBakerEffendi' # the username which created the personal access token
        repo: 'plume-oss/plume-driver' # the repo to publish to