site:
  about:
    siteName: Plume
    siteDescription: Projects bytecode into a code-property graph which is inserted it into a graph database. A type-safe interface for connecting and writing to various graph databases is provided. This based on the code-property graph schema.
Editorial:
  primaryColor: '#00bdd6'
  legacySearch: false
  metaComponents:
    - type: 'orchidSearch'
  social:
    github: 'plume-oss/plume'
  menu:
    - type: 'separator'
      title: 'API Docs'
    - type: 'sourcedocPages'
      moduleType: 'kotlindoc'
      node: 'classes'
      asSubmenu: true
      submenuTitle: 'Classes'
    - type: 'sourcedocPages'
      moduleType: 'kotlindoc'
      node: 'packages'
      asSubmenu: true
      submenuTitle: 'Packages'

kotlindoc:
  sourceDirs:
    - './../../main/kotlin'

services:
  publications:
    stages:
      - type: 'githubPages'
        username: 'DavidBakerEffendi' # the username which created the personal access token
        repo: 'plume-oss/plume' # the repo to publish to